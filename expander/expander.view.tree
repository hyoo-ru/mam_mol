$mol_expander $mol_list
	rows /
		<= Label $mol_float
			sub /
				<= Trigger $mol_check_expand
					checked?val <=> expanded?val false
					label <= label /
						<= title
				<= Tools null
		<= Content $mol_view
			sub <= content /
