namespace $ {
	
	console.warn( '$mol_atom_status is deprecated. Use `$mol_fiber_status` instead.' )

	export enum $mol_atom_status {
		obsolete = 'obsolete' ,
		checking = 'checking' ,
		pulling = 'pulling' ,
		actual = 'actual' ,
	}
	
	
}
