# $mol_cost

Prints [money unit](unit/money) in right format.

## Usage example

```tree
$mol_cost
	value <= cost $mol_unit_money
		valueOf 0
```

## Properties

**`value()` : $mol_unit_money**

Printed value.
