namespace $ {
	export class $mol_example extends $mol_view {
		
		/**
		 * ```tree
		 * tags /string
		 * ```
		 */
		tags() {
			return [
			] as readonly string[]
		}
		
		/**
		 * ```tree
		 * aspects /string
		 * ```
		 */
		aspects() {
			return [
			] as readonly string[]
		}
	}
	
}

