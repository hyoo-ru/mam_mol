namespace $ {
	export class $mol_check_group extends $mol_check_box {

		/**
		 * ```tree
		 * checks /$mol_check
		 * ```
		 */
		checks() {
			return [

			] as readonly $mol_check[]
		}

		/**
		 * ```tree
		 * full true
		 * ```
		 */
		full() {
			return true
		}
	}

}
