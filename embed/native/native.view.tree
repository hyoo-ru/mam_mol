$mol_embed_native $mol_scroll
	dom_name \object
	window null
	attr *
		^
		data <= uri?val \
		type <= mime \
	sub /
		<= Fallback_link $mol_link
			uri <= uri
			sub /
				<= Fallback_image $mol_image
					uri <= uri
					title <= title?val \
