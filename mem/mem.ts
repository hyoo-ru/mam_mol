namespace $ {
	export let $mol_mem = $mol_wire_mem(0)
	export let $mol_mem_key = $mol_wire_mem(1)
	export let $mol_mem_key_key = $mol_wire_mem(2)
	export let $mol_mem_key_key_key = $mol_wire_mem(3)
}
