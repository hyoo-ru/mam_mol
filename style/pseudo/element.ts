namespace $ {
	
	export type $mol_style_pseudo_element =
	
	// common
	| '::after'
	| '::before'
	| '::cue'
	| '::first-letter'
	| '::first-line'	
	| '::selection'
	| '::slotted'

	// experimental
	| '::backdrop'
	| '::placeholder'
	| '::marker'
	| '::spelling-error'
	| '::grammar-error'

}
